fn main() {
    println!("The `tdns-update` command has been superseded by the `tdns` command from the `tdns-cli` crate.");
    println!("See <https://github.com/rotty/tdns-cli> for detailed installation instructions.");
}
